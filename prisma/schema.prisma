generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Referral {
  referrerName String
  referrerEmail String
  refereeName  String
  refereeEmail String
}
